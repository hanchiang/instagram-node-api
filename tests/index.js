/* eslint-disable */
const { expect } = require('chai').expect;
const request = require('supertest');

const app = require('../app');

describe('get user media test', () => {
  expect(true).to.equal(true);
});